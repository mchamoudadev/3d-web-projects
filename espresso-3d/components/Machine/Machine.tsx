import Placeholder from "./Placeholder";

// A future GLB adapter only replaces this component's source. It must register
// the same fourteen local-origin groups through useMachineParts().
export default function Machine() {
  return <Placeholder />;
}
